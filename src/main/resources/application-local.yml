spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: courshare
    password: courshare
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
